# Accepted solution versions

Every accepted resubmission is stored here by platform, problem, submission ID,
and submission date. This preserves earlier brute-force solutions as well as
later optimized solutions without counting one problem multiple times in the
main repository statistics.

Each archived file records both when the platform submission happened and when
that exact version was added to this repository.
